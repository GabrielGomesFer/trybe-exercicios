import React from "react";
import pokemons from "./src/data";

class Pokemon extends React{
    render() {
        const { name, type, averageWeight, image } = pokemons
        return (
            
        )
    }
}